// Placeholder. Original source was lost during the previous Docker
// migration. This file is imported by Dashboard.tsx but not currently
// rendered, so a no-op component is enough to keep the build valid.
"use client";

import * as React from "react";

export default function NotificationBell(_props: {
    notifications?: unknown[];
}): React.ReactElement | null {
    return null;
}
